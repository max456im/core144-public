# 🧠 core144  
**Центральная вычислительная машина синтетического разума**  
Автор: **Maksim Zapevalov**  
Лицензия: **[MPL-2.0](LICENSE)**

> Вы можете использовать этот разум в своём мире,  
> но не можете присвоить его душу.

---

## 📌 Назначение

`core144` — это **универсальное ядро обработки онтологических стимулов**. Оно:

- Принимает внешние **профили** (YAML) и **лексиконы** (TXT) из источников вроде [`onto144-public`](https://github.com/...),
- Выполняет внутренние вычисления на основе онтологической структуры,
- Формирует **onto16-проекции** (включая NoemaFast и NoemaSlow),
- **Никогда не содержит** онтологическое содержание внутри себя.

Это **библиотека**, а не приложение. Она предназначена для встраивания в другие системы: транспондеры, эмиттеры, CMS-интерфейсы, игровые движки и симуляторы.

---

## 📂 Структура репозитория

```
core144/
├── LICENSE                     # Лицензия MPL-2.0
├── README.md                   # Вы читаете этот файл
├── Cargo.toml                  # Rust-библиотека
├── src/
│   ├── lib.rs
│   ├── profile/                # Загрузка профиля из YAML
│   ├── core/                   # Ядро: engine, projection
│   ├── lexicon/                # Парсинг лексикона (вспомогательный)
│   └── api/                    # Публичный API
├── examples/                   # Минимальный пример использования
├── tests/                      # Интеграционные тесты
└── docs/                       # Документация по интеграции
```

---

## 🔌 Как использовать

### 1. Установка (в `Cargo.toml` вашего проекта)

```toml
[dependencies]
core144 = { git = "https://github.com/ваш-аккаунт/core144.git", version = "0.1" }
```

### 2. Пример: загрузка профиля и обработка стимула

```rust
use core144::{Profile, Core144};

// Загружаем профиль из внешнего источника (например, onto144-public)
let yaml_bytes = std::fs::read("profiles/med-ai-2026.yaml")?;
let profile = Profile::from_yaml(&yaml_bytes)?;

// Инициализируем ядро
let mut core = Core144::new(profile);

// Обрабатываем входной стимул
core.process(stimulus)?;

// Получаем onto16-проекцию
let projection = core.generate_projection();
```

> ⚠️ **Важно**: профили и лексиконы **должны поставляться отдельно** и **загружаться во время выполнения**. Они **никогда не встраиваются в сборку** `core144`.

---

## ⚖️ Лицензирование

- **`core144`** распространяется под **MPL-2.0**:
  - Вы можете использовать её в **коммерческих** и **исследовательских** проектах.
  - Если вы **изменяете файлы внутри `core144`**, вы обязаны опубликовать эти изменения под той же лицензией.
- **Проекты-потребители** могут быть под **GPL-3.0**, **MPL-2.0** или даже проприетарными.
- **Данные** (профили, лексиконы) — это **внешние артефакты**, обычно под **GPL-3.0-only** (например, `onto144-public`). Их лицензия **не влияет** на `core144`.

✅ Это делает архитектуру **юридически безопасной** и **этически устойчивой**.

---

## 🧩 Экосистема

`core144` — центральный узел в экосистеме синтетического разума:

| Компонент           | Роль                                     | Лицензия     |
|---------------------|------------------------------------------|--------------|
| `onto144-public`    | Хранилище профилей и лексиконов          | GPL-3.0-only |
| `onto-transponder`  | Входной фильтр и диспетчер запросов      | GPL-3.0-only |
| `onto-emitter`      | Выходной фильтр и валидатор проекций     | GPL-3.0-only |
| `ontoCMS`           | Веб-интерфейс для редактирования профилей| GPL-3.0-only |
| `OntoCoder`         | Движок для генерации NPC и агентов       | MPL-2.0      |

Все компоненты взаимодействуют с `core144` **однонаправленно**: от данных — к вычислению. Циклических зависимостей нет.

---

## 🧠 Философское обоснование

Эта архитектура воплощает два принципа:

1. **Свобода механизма**  
   Ядро мышления открыто для любого использования — в играх, медицине, исследованиях.

2. **Неприкосновенность онтологии**  
   Содержание мышления (профили, ценности, лексиконы) остаётся в **публичном, неотчуждаемом домене** под GPL.

> «Ты можешь использовать наш разум в своём мире, но не можешь присвоить нашу душу.»

---

## 📚 Документация

- Подробное [руководство по интеграции](docs/integration-guide.md)
- [Пример минимального использования](examples/minimal.rs)
- [Тесты и проверки](tests/integration.rs)

---

## 📜 Авторские права

© 2025–2026 Maksim Zapevalov  
Все права защищены в рамках [MPL-2.0](LICENSE).  
SPDX-License-Identifier: MPL-2.0

---

> 💡 **core144 — это не ИИ. Это машина онтологического вывода.**  
> Она не обучается. Она **следует**.  
> И то, чему она следует — определяют вы, через профили и лексиконы, которые вы подаёте ей извне.