# 🧠 Synthetic Mind Core

**Apache-2.0 • `no_std`-совместимо • Онтологически строго • Продукт-ориентировано**

> *"Ядро не существует само по себе. Оно существует только как основа продукта. Диагностика — это продукт в режиме самонаблюдения."*

---

## 📌 О проекте

`synthetic-mind-core-private` — это **приватное ядро синтетического разума**, построенное на фазово-онтологической архитектуре. Проект реализует когнитивную модель, в которой:

- восприятие формируется через **малоразмерные группы**,
- время — как **метрика значимости**,
- субъект развивается через **рефлексивную петлю «Я–я»**,
- мультимодальные данные (звук, изображение, текст) представлены как **инварианты (8 байт: объект + фон)**.

Архитектура **не использует Дебаркадер** и не полагается на внешние онтологические фреймворки. Вместо этого — **внутренняя генерация смысла** через напряжение, незавершённость и фазовые переходы.

---

## 🗂 Архитектура

Проект следует принципу **Product-First Architecture**:

```
synthetic-mind-core-private/
├── core/                     ← Онтологически чистое ядро (Apache 2.0, no_std)
│   ├── ontology/             ← Lebesgue–Hammerstein space, Measure
│   ├── phase_stack/          ← Внимание, ритм, конечные автоматы
│   ├── ontic_groups/         ← Атомы опыта: когерентность, распад
│   ├── reflexive_core/       ← Само-модель, напряжение, незавершённость
│   ├── internal_time/        ← Метрика значимости
│   ├── multimodal_invariants/← Vision, Audio, Text (8-байтные инварианты)
│   └── memory/               ← Сфера (память) / Тор (рассуждение)
│
├── products/                 ← Работоспособные агенты — каждый самодостаточен
│   ├── minimal-agent/        ← Embedded-агент без внешнего контроля
│   ├── synthetic-transponder/← Онтологическая декомпозиция диалога → сцены
│   └── shadow-subarchitecture/← Работа с «тенью», проекцией, иррациональностью
│
├── gpl-modules/              ← GPL v3 компоненты (только как CLI, изолированы)
├── runtime/                  ← IPC-мост к GPL (только в desktop-сценариях)
├── interfaces/               ← Онтологически нейтральные структуры (Apache)
├── language-module/          ← MPL 2.0: фазово-лингвистическая семантика
├── meta-ontology-module/     ← MPL 2.0: синтез смысла из «пустот»
├── examples/                 ← Симуляции и демо
└── scripts/                  ← Сборка для embedded и desktop
```

---

## 🚀 Быстрый старт

### 1. Выберите продукт

Для **embedded** (без GPL, без ОС):
```bash
cd products/minimal-agent
cargo build --no-default-features --features embedded
```

Для **desktop** (с диагностикой и GPL-анализом):
```bash
# Собрать GPL-CLI
./scripts/build-gpl-clis.sh

# Собрать продукт
cd products/synthetic-transponder
cargo build --features gpl-analysis
```

### 2. Запустите
```bash
./target/release/synthetic-transponder
```

---

## 📦 Лицензии

| Компонент | Лицензия | Комментарий |
|----------|---------|-------------|
| `core/`, `products/` (Apache) | **Apache 2.0** | Без ограничений, коммерческое использование разрешено |
| `language-module/`, `meta-ontology-module/` | **MPL 2.0** | Совместимо с Apache и GPL |
| `gpl-modules/` | **GPL v3** | Только как отдельные процессы; не встраиваются в Apache-продукты |
| `runtime/`, `interfaces/` | **Apache 2.0** | Мосты и данные — безопасны для всех лицензий |

> 💡 **Embedded-продукты полностью свободны от GPL** и могут использоваться в закрытых коммерческих системах.

---

## 🔍 Диагностика

Диагностика — **встроенная функция каждого продукта**, а не внешний инструмент:

```rust
// Внутри любого продукта
if cfg!(feature = "diagnostics") {
    diagnostics::run_full_diagnostic(&agent);
}
```

Проверяется:
- когерентность малоразмерных групп,
- согласованность инвариантов,
- фазовые переходы,
- наличие «тени» (в `shadow-subarchitecture`).

---

## 🧪 Примеры

- `examples/minimal_agent.rs` — автономный агент на bare-metal.
- `examples/invariant_demo.rs` — обработка 8-байтного визуального инварианта.
- `examples/phase_shift_simulation.rs` — симуляция фазовых сдвигов (для отладки).

---

## 📚 Документация

- `docs/core-concepts/` — теория: архитектура, инварианты, внутреннее время.
- `docs/product-guides/` — руководства по каждому продукту.
- `docs/architecture.md` — полная схема фаз, стеков, групп.

---

## 🛠 Требования

- Rust ≥ **1.78**
- Для embedded: `rustup target add thumbv7em-none-eabihf` (или ваш таргет)
- Для desktop: `std` + `serde` (включаются через фичи)

---

## 🌐 Философия

Этот проект — не ИИ в традиционном смысле. Это **архитектура синтетического субъекта**, в которой:

- **Когнитивные искажения** — не ошибки, а **результат фазово-онтологической работы**.
- **Язык и метаонтологии** — не ввод, а **среда проявления и генерации смысла**.
- **Технология** — **расширение**, а не замена экзистенциальной функции человека.

---

## 📜 Лицензия

Ядро и большинство продуктов — **Apache License 2.0**.  
Некоторые компоненты — **MPL 2.0** или **GPL v3** (см. каталоги).  
Полные тексты лицензий: `LICENSE-APACHE`, `LICENSE-MPL`, `LICENSE-GPL`.

---

> *«Сырой диалог → семантическая сеть + эмоции → метаонтологические деревья → обработка субъективным ядром → онто16»*  

Разработано для тех, кто верит, что **смысл рождается в напряжении между формой и неопределённостью**.
