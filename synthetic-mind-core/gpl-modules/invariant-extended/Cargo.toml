```toml
[package]
name = "invariant-extended"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
interfaces = { path = "../../interfaces" }
serde = { version = "1.0", features = ["derive"] }
```