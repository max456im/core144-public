```toml
[package]
name = "synthetic-mind-interfaces"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

# Никаких зависимостей — только `no_std` и стандартные типы
[dependencies]

[features]
default = []
std = []
```