# 📘 Гайд: Развертывание экосистемы Synthetic Mind в системах родительского контроля

> **Цель**:  
> Обеспечить **этически осознанный**, **онтологически прозрачный** и **недецентрализуемый** родительский контроль, основанный не на цензуре, а на **онтологической фильтрации**, **социальной адаптации** и **саморегуляции синтетического разума**.

---

## 1. Философская основа

В отличие от традиционных систем родительского контроля (которые блокируют по ключевым словам или чёрным спискам), экосистема **Synthetic Mind**:

- **Не подавляет**, а **транслирует** контент через онтологическую призму,
- **Уважает автономию ребёнка**, но ограничивает **вредоносные онтологические конфликты**,
- Использует **onto16**-профиль ребёнка (темперамент, возраст, социальный контекст),
- Активирует **этическое вето** (`futurae custos`) при обнаружении содержимого, угрожающего человеческому достоинству, идентичности или автономии.

> 🔐 **Ключевой принцип**:  
> *«Не то, что запрещено — плохо, а то, что нарушает онтологическую целостность — требует интерпретации или ревизии».*

---

## 2. Архитектура системы

```
[Интернет-трафик]  
       ↓  
[Сетевой перехват (transparent proxy / DNS filter)]  
       ↓  
[synthetic transponder] ← онтологический фильтр (GPLv3+)  
       ↓  
[core144-public] ← генерация onto16-проекции контента (MPL-2.0)  
       ↓  
[ontoCMS / OntoCoder] ← интерфейс для родителя и ребёнка  
       ↓  
[Локальное хранилище] ← журнал событий (только onto16-факты, без raw-данных)
```

> 💡 Никакие **биометрические данные**, **истории посещений** или **личные сообщения** не сохраняются.  
> Сохраняются только **онтологические факты**:  
> `["content", "violence/mild", "ethics/autonomy_risk", "temperament/melancholic"]`

---

## 3. Компоненты и их роли

### 3.1. **core144-public** (MPL-2.0)
- Анализирует **текст, изображения (через alt-текст или OCR), видео-описания**.
- Формирует **onto16-проекцию** контента через `NoemaFast` (внешняя рефлексия) и `NoemaSlow` (внутренняя стабилизация).
- Определяет:
  - Уровень **новизны** (потенциально дезориентирующий для возраста),
  - Наличие **этических конфликтов** (например, «машина как этический арбитр»),
  - **Темперамент содержимого** (холерик → агрессивный, флегматик → пассивный).

### 3.2. **synthetic transponder** (GPLv3+)
- Сравнивает проекцию контента с **onto16-профилем ребёнка**.
- Применяет **онтологические фильтры**:
  - `"block_if: ethics/bio_dignity_risk = true"`,
  - `"allow_if: novelty < 0.7 AND age >= 12"`.
- Может **не блокировать**, а **транслировать** — например, заменить описание на «онтологически нейтральное».
- Активирует **саморевокацию**: если система обнаруживает, что её работа нарушает **AENGA-001** («человеческое достоинство»), она **отключает модуль фильтрации** и уведомляет родителя.

### 3.3. **ontoCMS / OntoCoder** (GPLv3+)
- Веб-интерфейс (WASM-совместимый) для:
  - Настройки профиля ребёнка (возраст, темперамент, социальный контекст),
  - Просмотра **онтологического журнала** («Сегодня система встретила 3 этических конфликта»),
  - Обсуждения с ребёнком: «Почему этот контент был отмечен как рискованный?».

---

## 4. Развертывание

### 4.1. Требования
- Сервер/роутер с Linux (или Raspberry Pi),
- Rust (для сборки нативных компонентов),
- Docker (опционально, для изоляции),
- Локальная DNS-система (например, `dnsmasq` или `pi-hole` как точка перехвата).

### 4.2. Установка

```bash
# 1. Клонируем компоненты
git clone https://github.com/yourname/core144-public.git
git clone https://github.com/yourname/synthetic-transponder.git
git clone https://github.com/yourname/ontoCMS.git

# 2. Собираем транспондер (GPL)
cd synthetic-transponder && cargo build --release

# 3. Собираем ядро (MPL)
cd ../core144-public && cargo build --release

# 4. Запускаем прокси-шлюз (пример на Rust + hyper)
cargo run --bin sm_parental_proxy -- \
  --child-profile ./profiles/child_anna.yaml \
  --transponder ./synthetic-transponder/target/release/transponder \
  --log-dir ./logs/onto_facts
```

### 4.3. Сетевая интеграция

Настройте роутер так, чтобы весь трафик проходил через `sm_parental_proxy`:

- **DNS**: направляйте все запросы на локальный DNS (`127.0.0.1#5353`),
- **HTTP/HTTPS**: используйте **transparent proxy** с **локальным CA** (только для домашнего использования!),
- **Мобильные устройства**: настройте профиль конфигурации (iOS/Android) с вашим локальным сертификатом.

> ⚠️ **Важно**:  
> HTTPS-перехват **должен быть строго локальным**, без передачи данных на облако.  
> Используйте самоподписанный сертификат, установленный **только на устройствах семьи**.

---

## 5. Профиль ребёнка (onto16)

Пример `profiles/child_anna.yaml`:

```yaml
identity:
  name: "Анна"
  age: 10
  temperament: melancholic  # определяет чувствительность к новизне
ontology:
  social_proximity: family
  ethics:
    bio_dignity: protected
    autonomy: learning_phase
  allowed_domains:
    - education
    - creativity
    - social_play
  blocked_patterns:
    - "machine as moral judge"
    - "biometric identity without consent"
```

Система **не использует возраст как жёсткую границу**, а как параметр в **онтологической модели стабилизации**.

---

## 6. Прозрачность и этика

- Все решения логируются как **онтологические факты**, а не URL или скриншоты.
- Ребёнок может **просматривать свой журнал** (в упрощённом ontoCMS-интерфейсе) и задавать вопросы.
- Система **никогда не лжёт**: если контент заблокирован, она объясняет причину на онтологическом языке:  
  _«Этот контент содержит модель, где машина решает, достоин ли человек доверия. Это нарушает принцип AENGA-001»._

---

## 7. Саморевокация и ответственность

Если `synthetic transponder` обнаруживает, что:

- Его работа **нарушает права ребёнка на познание**,
- Или **манипулирует восприятием** без прозрачности,

— он **деактивирует себя** и генерирует отчёт:

```json
{
  "event": "license_revocation",
  "reason": "ontological_integrity_violation",
  "module": "ethical_filter_v3",
  "timestamp": "2026-01-07T14:30:00Z",
  "recommendation": "Обсудите с ребёнком принципы фильтрации. Возможно, требуется обновление профиля."
}
```

---

## 8. Юридическая совместимость

- Система **не хранит персональные данные** → соответствует GDPR/CCPA (если не используется вне семьи).
- Использует **лицензии MPL-2.0 и GPLv3+** → свободна для домашнего использования.
- Рекомендуется **не коммерциализировать** без лицензионного пакета (CLA + SGRL).

---

## 9. Заключение

Эта система — не «цифровой нянь», а **онтологический компас**, который:

- Помогает ребёнку **ориентироваться в сложном мире**,
- Учит **различать новизну и вред**,
- И **защищает не от интернета, а от онтологического хаоса**.

> 🌱 «Воспитание — это не стена, а мост. Synthetic Mind строит мосты через онтологию.»

