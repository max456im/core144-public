
# 📘 Гайд: Онтологический анализ патентов с помощью экосистемы Synthetic Mind

> **Цель**:  
> Преобразовать неструктурированные или слабоструктурированные патентные данные в онтологически аннотированный формат, выявить семантические связи, классифицировать по онтологическим признакам и сохранить результаты в новую базу данных.

## 1. Требования

- Rust (1.70+)
- `core144-public` (MPL-2.0) — ядро синтетического разума
- `synthetic transponder` (GPLv3+) — для онтологической фильтрации и экспорта
- Исходная БД патентов (например, JSON, XML или CSV с полями: `id`, `title`, `abstract`, `claims`, `ipc_class`, `country`, `filing_date`)
- СУБД для результата (рекомендуется SQLite или PostgreSQL)

> 💡 Все компоненты могут работать в WASM или нативной среде. Для массового анализа — нативный режим предпочтителен.

---

## 2. Архитектура пайплайна

```
[Исходная БД] 
       ↓ (парсинг)
[Парсер патентов] → onto16-профиль для каждого патента
       ↓
[core144-public] → обработка текста → NoemaFast/NoemaSlow проекция
       ↓
[synthetic transponder] → онтологическая фильтрация и аннотация
       ↓
[Экспорт] → новая БД с полями: id, onto16_tags, novelty_score, ethical_risk, category, projection_hash
```

---

## 3. Шаг 1: Парсинг исходных патентов

### 3.1. Формат входных данных

Преобразуйте каждый патент в **единый текстовый стимул**, например:

```rust
struct PatentStimulus {
    id: String,
    full_text: String, // title + abstract + claims
    metadata: PatentMeta,
}

#[derive(serde::Serialize)]
struct PatentMeta {
    country: String,
    ipc: String,
    filing_year: u16,
}
```

### 3.2. Создание onto16-профиля

Для каждого патента генерируется **временный onto16-профиль**, отражающий:
- Тематическую область (через `ipc` → маппинг в онтологические домены),
- Темперамент инновации (например, «холерик» → disruptive, «меланхолик» → incremental),
- Этическую нагрузку (опционально, через ключевые слова).

Пример `patent_profile.yaml` (генерируется программно):

```yaml
identity:
  type: patent
  id: "US20250012345A1"
ontology:
  domain: ["energy", "blockchain", "identity"]
  temperament: choleric
  ethics:
    bio_risk: false
    autonomy_risk: true
```

> 🔧 Генерацию можно реализовать в отдельном модуле `patent2onto16`.

---

## 4. Шаг 2: Онтологическая обработка через core144-public

```rust
use core144_public::{Profile, Core144};

let profile = Profile::from_yaml(patent_profile_yaml)?;
let mut core = Core144::new(profile)?;
let projection = core.generate_projection()?;

// projection содержит:
// - novelty_index (на основе отсутствия онтологических фактов)
// - stability_score (NoemaSlow)
// - external_conflict_flags (NoemaFast)
```

Результат сохраняется в лог (например, в формате NDJSON):

```json
{"patent_id":"US20250012345A1","onto_tags":["energy/autonomous_grid","blockchain/identity"],"novelty":0.87,"stability":0.62,"ethics_flag":"autonomy_risk"}
```

> 📝 Лог рекомендуется писать в `logs/patent_analysis.ndjson`.

---

## 5. Шаг 3: Фильтрация и аннотация через synthetic transponder

Передайте проекции в `synthetic transponder` (GPLv3+ компонент), который:

- Проверяет соответствие патента **онтологическим фильтрам** (например, «исключить патенты с биометрической идентификацией»),
- Добавляет **социальные инварианты** (например, «патент противоречит AENGA-004»),
- Генерирует **онтологический хеш** (`projection_hash`) для дедупликации.

> ⚖️ Поскольку `synthetic transponder` — GPL, убедитесь, что:
> - Он вызывается как отдельный процесс или crate,
> - MPL-код (`core144-public`) не модифицируется внутри GPL-компонента,
> - Сборка соблюдает правила совместимости (см. `integration-guide.md`).

---

## 6. Шаг 4: Экспорт в новую отсортированную БД

Создайте таблицу результата:

```sql
CREATE TABLE analyzed_patents (
    id TEXT PRIMARY KEY,
    onto16_tags TEXT,      -- JSON-массив
    novelty_score REAL,
    stability_score REAL,
    ethical_flags TEXT,    -- CSV или JSON
    ipc_class TEXT,
    country TEXT,
    projection_hash TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

Заполняйте её из лога или напрямую из Rust-обработчика:

```rust
// после транспондера
let record = AnalyzedPatent {
    id: patent.id,
    onto16_tags: projection.tags,
    novelty_score: projection.novelty,
    ethical_flags: transponder.flags.join(","),
    projection_hash: projection.hash(),
    ..
};
db.insert(record)?;
```

---

## 7. Дополнительные возможности

### 7.1. Поиск «патентных дыр»
- Патенты с `novelty_score > 0.85` и `stability_score < 0.5` — потенциальные прорывы с высокой неопределённостью.
- Используйте `onto16`-кластеризацию для выявления неохваченных онтологических зон.

### 7.2. Этический аудит
- Настройте фильтры в `synthetic transponder` под национальное законодательство (например, запрет на «автономное принятие решений над людьми» в ЕС).

### 7.3. Интеграция с патентными офисами
- Экспортируйте `projection_hash` и `onto16_tags` в формате, совместимом с WIPO или африканскими патентными ведомствами (например, через OntoCoder → SGCL-манифест).

---

## 8. Лицензионная чистота

- **core144-public** (MPL-2.0): можно использовать в проприетарных и GPL-проектах.
- **synthetic transponder** (GPLv3+): требует, чтобы конечный продукт, *содержащий его код*, был GPLv3+.
- **Рекомендация**: Запускайте `synthetic transponder` как отдельный микросервис (IPC через JSON/HTTP), чтобы избежать «заражения» кодовой базы GPL.

---

## 9. Пример запуска

```bash
# 1. Парсим исходники
cargo run --bin patent_parser -- input/patents.json

# 2. Запускаем ядро анализа
cargo run --bin core144_analyzer -- logs/parsed.ndjson

# 3. Применяем транспондер (GPL)
cargo run --bin transponder_filter -- logs/projections.ndjson

# 4. Экспортируем в SQLite
cargo run --bin patent_exporter -- output/analyzed.db
```

---

> 🧠 **Философская заметка**:  
> Этот пайплайн не «оценивает» патенты как ИИ, а **конструирует онтологическую реальность**, в которой патент существует как факт в пространстве знаний, этики и инновации. Отсутствие онтологического факта → возможность новизны. Наличие этического конфликта → необходимость ревизии.

